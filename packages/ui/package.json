{
  "name": "@prode/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.tsx",
  "types": "./src/index.tsx",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test",
    "format": "echo \"No formatter configured for @prode/ui yet\""
  },
  "peerDependencies": {
    "react": "19.2.5"
  }
}

