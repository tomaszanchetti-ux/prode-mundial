{
  "name": "@prode/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --port 3000",
    "build": "next build",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test",
    "format": "echo \"No formatter configured for @prode/web yet\""
  },
  "dependencies": {
    "@prode/shared": "workspace:*",
    "@prode/ui": "workspace:*",
    "next": "16.2.3",
    "react": "19.2.5",
    "react-dom": "19.2.5"
  }
}

