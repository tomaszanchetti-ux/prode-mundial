import { Card } from "@prode/ui";

export default function RulesPage() {
  return (
    <main style={{ maxWidth: 720, margin: "0 auto", padding: "40px 20px 56px" }}>
      <Card>
        <h1 style={{ marginTop: 0 }}>Reglas y puntos</h1>
        <p>Predicción cerrada al kickoff exacto, competencia por ligas y scoring resuelto siempre por backend.</p>
      </Card>
    </main>
  );
}
