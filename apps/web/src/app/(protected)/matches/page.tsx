import { Card } from "@prode/ui";

export default function MatchesPage() {
  return (
    <Card>
      <h1 style={{ marginTop: 0 }}>Partidos</h1>
      <p style={{ marginBottom: 0 }}>
        Placeholder funcional para la lista de fixtures. Epic 2 conectará filtros, estados editable/bloqueado y guardado.
      </p>
    </Card>
  );
}
